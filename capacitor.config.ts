import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Strictly Business',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
